import { SafeAreaView, Text } from "react-native";

const SearchPillScreen = () => {
  return (
    <SafeAreaView>
      <Text>알약 검색</Text>
    </SafeAreaView>
  );
}

export default SearchPillScreen;